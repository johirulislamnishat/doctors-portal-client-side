import React from 'react';
import NavBar from '../../Shared/NavBar';

const Appointment = () => {
    return (
        <div>
            <NavBar />

        </div>
    );
};

export default Appointment;